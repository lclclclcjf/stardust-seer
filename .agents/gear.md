# Gear Notes

- The repository's documented deployment path is GitHub `master` to an automatic Vercel production deployment.
- For animation-only changes, verify ESLint, `next build`, diff whitespace, and the live interaction states before pushing.
